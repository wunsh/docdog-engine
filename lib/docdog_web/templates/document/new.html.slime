- project_id = @conn.params["project_id"]

h2 New Document

= render "form.html", Map.put(assigns, :action, project_document_path(@conn, :create, project_id))

span = link "Back", to: project_document_path(@conn, :index, project_id)
